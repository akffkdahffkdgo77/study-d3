import BarChart from 'components/Bar/BarChart';
import { data, options } from 'components/Bar/BarChart/data';

export default function BarChartDemo() {
    return <BarChart data={data} options={options} />;
}
