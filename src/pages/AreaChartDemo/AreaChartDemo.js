import AreaChart from 'components/AreaChart';
import { data, options } from 'components/AreaChart/data';

export default function AreaChartDemo() {
    return <AreaChart data={data} options={options} />;
}
